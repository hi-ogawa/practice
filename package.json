{
  "dependencies": {
    "eslint": "^7.4.0",
    "eslint-config-google": "^0.14.0"
  },
  "eslintConfig": {
    "env": {
      "node": true,
      "es6": true
    },
    "extends": ["eslint:recommended", "google"],
    "rules": {
      "max-len": [ "error", { "ignoreComments": true } ]
    }
  }
}
